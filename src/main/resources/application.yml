
spring:
  datasource:
    url: jdbc:mysql://localhost:3305/quiz-servicedb
    username: quizuser
    password: quizuser
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: Quiz-Service
#    properties:
#      hibernate:
#        format_sql: true
#        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8090

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    hostname: localhost
